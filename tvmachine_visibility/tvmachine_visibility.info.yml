name: TVMachine visibility
type: module
description: Allow users to hide some channels or channel groups from listings (cookies-based approach).
core_version_requirement: ^8.8 || ^9
package: tvmachine
# core: 8.x
dependencies:
  - drupal:block
  - drupal:node
version: '8.x-1.x-dev'